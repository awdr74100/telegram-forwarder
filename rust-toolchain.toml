# Pins the toolchain the same way `.node-version` pinned Node.
# `rustup` reads this file automatically, so `cargo build` in a fresh clone
# installs and uses the right toolchain without any manual step.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
